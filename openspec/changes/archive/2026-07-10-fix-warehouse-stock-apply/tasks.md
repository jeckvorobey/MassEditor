## 1. Выполнено ранее

- [x] 1.1 Сверить поведение с официальной документацией Shop-Script по `shop_product_stocks`, агрегатам остатков и `waModel`.
- [x] 1.2 Обновить PHP-тест: выбранный склад у товара без складских строк создает строку только выбранного склада и не пишет новое значение напрямую в `shop_product_skus.count`.
- [x] 1.3 Добавить PHP-тесты режимов `increase`, `decrease` и `infinite` для выбранного склада от существующего значения или `0`.
- [x] 1.4 Добавить PHP-тест отказа при `decrease` ниже нуля и сохранения строк других складов.
- [x] 1.5 Подтвердить PHP-тестом прежнее поведение без выбранного склада: общий `sku.count` меняется только у товаров без складского учета, складские товары отклоняются.

## 2. Выполнено ранее

- [x] 2.1 Изменить выбор ветки операции `stock`: `stock_id > 0` всегда ведет в складскую операцию.
- [x] 2.2 Обновить `applyWarehouseStockOperation()` так, чтобы он писал только выбранный `stock_id` через placeholders и не создавал строки других складов.
- [x] 2.3 После складской записи обновлять `edit_datetime` товара и вызывать `shopProductModel->correct(product_id)`.
- [x] 2.4 Не менять UI, JS, локализацию, схему БД и фильтр товаров.

## 3. Выполнено ранее

- [x] 3.1 Запустить `php -l wa-apps/shop/plugins/masseditor/lib/classes/shopMasseditorPluginMassOperationService.class.php`.
- [x] 3.2 Запустить `bash tests/run-php-tests.sh --filter MassOperationServiceTest`.
- [x] 3.3 Запустить `bash tests/run-php-tests.sh`.
- [x] 3.4 Запустить `openspec validate --strict --all`.

## 4. Исправление ошибки реальной схемы

- [x] 4.1 Добавить падающий PHP-тест: складская запись содержит `product_id` выбранного товара для каждого SKU.
- [x] 4.2 Исправить пакетную SQL-запись `applyWarehouseStockOperation()` — передавать `product_id` вместе с `sku_id`, `stock_id` и `count` через именованные плейсхолдеры.
- [x] 4.3 Убедиться PHP-тестом, что запись нескольких SKU не смешивает `product_id` и не затрагивает другие склады.

## 5. Проверки

- [x] 5.1 Запустить `php -l wa-apps/shop/plugins/masseditor/lib/classes/shopMasseditorPluginMassOperationService.class.php`.
- [x] 5.2 Запустить `bash tests/run-php-tests.sh --filter MassOperationServiceTest`.
- [x] 5.3 В Docker-стенде массово назначить остаток выбранному складу товару без складских строк и подтвердить отсутствие ошибки, создание строк с `product_id` и пересчет агрегатов.
- [x] 5.4 Запустить `openspec validate --strict --all`.

## 6. Общие остатки и клиентская валидация

- [x] 6.1 Добавить JS-тест: для выделенного товара со складским учетом выбор «Без склада» подсвечивает селект и не открывает окно подтверждения.
- [x] 6.2 Добавить PHP-тест: при установке остатка на выбранном складе сервис материализует нулевые строки остальных складов и передает `product_id`.
- [x] 6.3 Добавить в разметку безопасный признак складского учета выбранного товара и CSS-класс красной подсветки селекта.
- [x] 6.4 Реализовать JS-валидацию выбранного склада перед открытием окна подтверждения; при выборе склада снимать подсветку.
- [x] 6.5 Восстановить пакетную запись нулевых строк остальных складов, чтобы `shopProductModel->correct()` вычислял общий остаток числом.
- [x] 6.6 Запустить JS- и PHP-тесты, `php -l` и `openspec validate --strict --all`.
