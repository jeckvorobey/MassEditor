## 1. Базовая проверка до пересборки

- [ ] 1.1 Повторно сверить актуальные требования Webasyst Store к `tar.gz`, единственной корневой директории, `plugin.php`, `.htaccess`, PREMIUM-декларации, локализациям и тестированию обновлений по официальным источникам из proposal.
- [ ] 1.2 Зафиксировать исходный `git status`, полный manifest `wa-apps/shop/plugins/masseditor/`, версию и числовой `vendor` из `lib/config/plugin.php`, а также содержимое `lib/config/shop_support.json`; не изменять существующие пользовательские правки вне упаковки.
- [ ] 1.3 Выполнить красную проверку: распаковать текущий `dist/masseditor-1.1.0.tar.gz` во временную директорию и подтвердить, что рекурсивное сравнение с production-деревом завершается различиями.

## 2. Runtime и compliance preflight

- [ ] 2.1 Запустить `php -l` для всех PHP-файлов production-дерева и `node --check wa-apps/shop/plugins/masseditor/js/masseditor.js`; остановить сборку при любой syntax error.
- [ ] 2.2 Запустить `bash tests/run-php-tests.sh` и `bash tests/run-js-tests.sh`; не собирать финальный архив при падении тестов.
- [ ] 2.3 Проверить production-дерево на пустые директории, абсолютные/выходящие наружу symlink, временные/резервные файлы, debug-вызовы, hardcoded `/webasyst/`, запрещенные файловые записи и очевидные SQL/XSS/metadata/install-uninstall packaging-блокеры по compliance checklist.
- [ ] 2.4 Проверить точное содержимое корневых `lib/.htaccess`, `templates/.htaccess`, `locale/.htaccess`, наличие всех production-config файлов и отсутствие файлов ядра Webasyst/Shop-Script внутри дерева плагина.

## 3. Подготовка локализаций

- [ ] 3.1 Проверить доступность доверенного gettext-совместимого `msgfmt`; если инструмент недоступен, остановить сборку без замены старого архива.
- [ ] 3.2 Скомпилировать с проверкой формата `ru_RU` и `en_US` файлы `shop_masseditor.po` в соответствующие `shop_masseditor.mo`, затем подтвердить наличие и ненулевой размер обеих бинарных локализаций.
- [ ] 3.3 Повторно запустить локализационные PHP/JS-тесты или полные suites, если пересборка `.mo` изменила production-файлы.

## 4. Формирование кандидата

- [ ] 4.1 Создать временные staging- и output-директории, зарегистрировать их очистку при успехе/ошибке и скопировать production-дерево целиком как единственную директорию `masseditor/` с сохранением скрытых `.htaccess`.
- [ ] 4.2 До упаковки проверить staging-manifest на полное совпадение с источником, отсутствие пустых директорий, специальных файлов, опасных symlink и материалов разработки (`docs`, `tests`, `openspec`, Docker, Git/IDE/Codex-файлы).
- [ ] 4.3 Собрать временный `tar.gz` кандидат для версии, прочитанной из `plugin.php`, не заменяя `dist/masseditor-1.1.0.tar.gz`.

## 5. Проверка и замена архива

- [ ] 5.1 Проверить кандидата через `gzip -t` и `tar`, подтвердить единственный верхнеуровневый каталог `masseditor/`, безопасные относительные пути и отсутствие второго корня или лишних файлов.
- [ ] 5.2 Распаковать кандидат в новую временную директорию; рекурсивно сравнить его с `wa-apps/shop/plugins/masseditor/` и повторно проверить `plugin.php`, `shop_support.json`, `.htaccess`, пары `.po/.mo` и production-only manifest.
- [ ] 5.3 После всех зеленых проверок атомарно заменить `dist/masseditor-1.1.0.tar.gz` проверенным кандидатом; при ошибке сохранить прежний архив и удалить неуспешный кандидат.

## 6. Финальная верификация

- [ ] 6.1 Повторно проверить уже финальный архив командами `gzip -t`, `tar -tzf` и распаковкой с нулевым diff относительно production-дерева.
- [ ] 6.2 Вывести и сохранить в итоговом отчете путь `dist/masseditor-1.1.0.tar.gz`, размер, число файлов и SHA-256; явно подтвердить, что архив не был загружен во внешний Webasyst Store.
- [ ] 6.3 Сопоставить финальный `git status` с исходным, выполнить `git diff --check` и `openspec validate --strict --all`; убедиться, что вне ожидаемых `.mo`, `dist/` и OpenSpec-артефактов не появилось новых изменений.
