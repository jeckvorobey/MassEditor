## 1. TDD: тесты ожидаемого поведения

- [x] 1.1 Добавить PHP-тест `getPage()` для `stock_id > 0`: агрегат товара берется только по выбранному складу, `stock_details` содержит только его запись, а другие склады не попадают в результат.
- [x] 1.2 Добавить PHP-тест выбранного склада с нулевым остатком и отдельный тест с бесконечным остатком SKU, проверив значения `0` и `null`/`∞`.
- [x] 1.3 Добавить PHP-тест регрессии для `stock_id = 0`: общий `product.count` и детали всех складов сохраняются.
- [x] 1.4 Добавить проверку backend-представления для desktop- и mobile-разметки: при выбранном складе кнопка `Склады` не рендерится, без фильтра остается доступной.

## 2. Серверная подготовка остатков

- [x] 2.1 Изменить вызов `attachStockDetails()` в `shopMasseditorPluginProductSelectionService::getPage()`, передав нормализованный `stock_id`.
- [x] 2.2 Расширить batch-логику `attachStockDetails()` параметром выбранного склада и добавить безопасное условие `ps.stock_id = i:selected_stock_id` только для `stock_id > 0`.
- [x] 2.3 В режиме выбранного склада установить отображаемый `product.count` по агрегату выбранного склада и сформировать одну `stock_details`-запись с корректным `count`/`count_view`.
- [x] 2.4 Сохранить текущую ветку `stock_id = 0` для общего остатка и всех складских деталей, не добавляя N+1-запросы.
- [x] 2.5 Убедиться, что отсутствие строки выбранного склада для прошедшего фильтр товара отображается как нулевой остаток, а `count IS NULL` сохраняется как бесконечный остаток.

## 3. Backend UI

- [x] 3.1 В desktop-ячейке остатка в `templates/actions/backend/Backend.html` выводить кнопку `Склады` и popover только при `filters.stock_id = 0`.
- [x] 3.2 Повторить то же условие в mobile-ячейке остатка и сохранить отображение самого остатка выбранного склада.
- [x] 3.3 Проверить, что существующие классы, ARIA-атрибуты, JS-поведение popover и локализация `stock_warehouses` не изменяются для режима `Все склады`.

## 4. Проверки и документационная сверка

- [x] 4.1 Сверить реализацию с официальными документами Webasyst по структуре плагинов, backend actions, классам и безопасному доступу к данным через `waModel`.
- [x] 4.2 Запустить `php -l` для измененных PHP-файлов.
- [x] 4.3 Запустить `bash tests/run-php-tests.sh` и убедиться, что новые тесты проходят.
- [x] 4.4 Запустить `openspec validate --strict --all`.
- [ ] 4.5 Выполнить ручную проверку в Docker: без фильтра видны общий остаток и кнопка `Склады`, при выборе `Склад 1` виден только его остаток и кнопка скрыта.
