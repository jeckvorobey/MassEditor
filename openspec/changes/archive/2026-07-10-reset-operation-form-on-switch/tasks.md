## 1. Регрессионные тесты

- [x] 1.1 Добавить JS-тест: переход на другую операцию очищает text/textarea и возвращает select к первому варианту.
- [x] 1.2 Добавить JS-тест: при смене операции сохраняются выбранные товары и при повторном выборе активной операции ее значения не очищаются.

## 2. Реализация

- [x] 2.1 Добавить в `masseditor.js` минимальный сброс controls `data-operation-fields` только при фактической смене операции.
- [x] 2.2 После сброса сохранить существующую корректную видимость зависимых полей и убрать состояние ошибки выбора склада.

## 3. Проверки

- [x] 3.1 Запустить `node --check wa-apps/shop/plugins/masseditor/js/masseditor.js` и `bash tests/run-js-tests.sh`.
- [x] 3.2 Запустить `openspec validate --strict --all`.
