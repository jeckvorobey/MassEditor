## 1. Регрессионные тесты языкового контракта

- [x] 1.1 Добавить JS-тест, который при `ru_RU` воспроизводит валидацию остатка без выбранного склада для товара со складским учетом и ожидает русский заголовок, текст и `aria-label` toast.
- [x] 1.2 Добавить JS-тест того же сценария при `en_US` и проверить английский комплект текста.
- [x] 1.3 Добавить PHP-тест `shopMasseditorPluginI18nService`, подтверждающий экспорт обязательных toast-ключей для `ru_RU` и `en_US`, включая ключ ошибки складского учета.

## 2. Локализация уведомлений

- [x] 2.1 Дополнить `$jsKeys` в `shopMasseditorPluginI18nService` ключом `validation_stock_required_for_accounted_products`, не добавляя русские fallback-строки в JavaScript.
- [x] 2.2 Сверить наличие и актуальность соответствующего ключа в `.po`/`.mo` для `ru_RU` и `en_US`; пересобирать `.mo` только при изменении `.po`.
- [x] 2.3 Проверить, что другой обход i18n-слоя отсутствует и `js/masseditor.js` сохраняет dependency-free подход с английскими защитными fallback-строками.

## 3. Постоянное правило OpenSpec

- [x] 3.1 Обновить `openspec/config.yaml`: при изменении видимого backend-текста требовать проверки `ru_RU` и `en_US`, источника i18n-ключа и теста отображения выбранного языка.
- [x] 3.2 Обновить `OPENSPEC_ROADMAP.md` и основной `openspec/specs/backend-ui-i18n/spec.md`, добавив чеклист и нормативный языковой контракт для уведомлений, валидаций и доступных подписей.

## 4. Проверки

- [x] 4.1 Запустить `node --check wa-apps/shop/plugins/masseditor/js/masseditor.js` и `bash tests/run-js-tests.sh`.
- [x] 4.2 Запустить `php -l` для каждого измененного PHP-файла и `bash tests/run-php-tests.sh` (или сфокусированный запуск с последующим полным запуском).
- [x] 4.3 Проверить актуальность `.po`/`.mo`, отсутствие русских строк в JS fallback и соответствие текстов `ru_RU`/`en_US` требованиям delta-spec.
- [x] 4.4 Запустить `openspec validate --strict --all`.
