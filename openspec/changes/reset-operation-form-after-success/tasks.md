## 1. Регрессионное поведение action

- [x] 1.1 Добавить PHP-регрессионный тест: успешная операция возвращает дефолтную форму, ошибка сохраняет POST-параметры.
- [x] 1.2 Запустить новый тест и подтвердить его падение до исправления.

## 2. Реализация

- [x] 2.1 Убрать восстановление применённых параметров формы только из успешной ветки backend action.
- [x] 2.2 Запустить PHP-регрессию и полный PHP-набор, проверить синтаксис изменённого PHP-файла.

## 3. Контроль качества

- [x] 3.1 Повторно сверить action и diff с документацией Webasyst и локальным контрактом action.
- [x] 3.2 Выполнить `complexity-optimizer`, затем `secure-review-loop`; устранить подтверждённые замечания через TDD.
- [x] 3.3 Выполнить релевантные полные проверки, `git diff --check` и `openspec validate --strict --all`.
