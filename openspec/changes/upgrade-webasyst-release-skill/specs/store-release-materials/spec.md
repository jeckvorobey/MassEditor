## ADDED Requirements

### Requirement: Единый доказательный источник release-документов
Store description, release note, changelog и publication checklist MUST формироваться из общего проектного каталога возможностей и манифеста конкретной версии. Каждый публичный факт MUST иметь evidence в коде, тесте или подтверждённой ручной проверке, а внутренние технические факты MUST храниться отдельно от Store-текста.

#### Scenario: Кумулятивное описание и delta версии
- **WHEN** готовятся документы новой версии
- **THEN** Store description ДОЛЖНО содержать все актуальные подтверждённые возможности продукта
- **AND** release note ДОЛЖНО содержать только изменения этой версии без фактов предыдущих выпусков
- **AND** changelog ДОЛЖЕН сохранить каждую опубликованную версию ровно один раз

#### Scenario: Публичный и engineering-текст
- **WHEN** release evidence содержит Docker-порт, имя внутреннего метода, refactor или тестовую настройку без пользовательского эффекта
- **THEN** этот факт MUST NOT попадать в Store description или публичную update note
- **AND** MAY быть сохранён в engineering report

### Requirement: Локали определяются из продукта
Release workflow MUST обнаруживать поддерживаемые локали по конфигурации и файловой структуре целевого продукта и MUST создавать документы с одинаковыми версиями, датами, разделами и смысловыми фактами для каждой обнаруженной локали без жёсткого ограничения `ru_RU`/`en_US`.

#### Scenario: Дополнительная локаль
- **WHEN** продукт кроме русской и английской локали содержит ещё одну поддерживаемую локаль
- **THEN** Store description, release note и changelog ДОЛЖНЫ быть сформированы и проверены также для этой локали

#### Scenario: Неполный перевод
- **WHEN** обязательный раздел или смысловой пункт отсутствует хотя бы в одной локали либо содержит незаполненный placeholder
- **THEN** release readiness ДОЛЖНА быть заблокирована с указанием локали и документа

### Requirement: Структурированные шаблоны документации
Bundled templates MUST задавать единый порядок разделов, но MUST NOT хранить конкретные возможности MassEditor. Фактические группы интерфейса, операции, фильтры, ограничения и доказанные границы отката ДОЛЖНЫ подставляться после анализа целевого продукта.

#### Scenario: Заполнение Store description
- **WHEN** target-product имеет подтверждённые группы интерфейса и функции
- **THEN** описание ДОЛЖНО расположить их в стабильном пользовательском порядке, отделить выбор/фильтры и контроль операций и использовать универсальную формулировку действий без вручную поддерживаемого перечня в «Как это работает»

#### Scenario: Откат не доказан
- **WHEN** интерфейс содержит кнопку отката, но code/tests не подтверждают поддерживаемые операции, срок, конфликт и права
- **THEN** Store description MUST NOT обещать откат и ДОЛЖНО зафиксировать его как неподтверждённую возможность
